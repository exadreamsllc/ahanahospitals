import SaasLandingPage from "@/app/saas/page";

// Export the SaaS landing page to be accessible under tenant folders too
// (e.g. for Vercel domain subdirectory redirects: /tenants/[tenant]/saas)
export default SaasLandingPage;
